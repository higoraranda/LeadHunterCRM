# Copie para .env.local (dev) ou defina nas Environment Variables da Vercel (produção).
# URL pública do backend hospedado no Render (sem barra no final, sem /api):
VITE_API_URL=https://leadhunter-backend.onrender.com
